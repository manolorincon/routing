<p id="notice"><%= notice %></p>

<h1>Drivers</h1>
<br>
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Email</th>
      <th>Telefono</th>
      <th>Max paradas</th>
      <th>Vehiculo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @drivers.each do |driver| %>
      <tr>
        <td><%= driver.nombre %></td>
        <td><%= driver.email %></td>
        <td><%= driver.telefono %></td>
        <td><%= driver.max_paradas %></td>
        <td><%= driver.max_paradas %></td>
        <td><%= link_to 'Ver', driver %></td>
        <td><%= link_to 'Editar', edit_driver_path(driver) %></td>
        <td><%= link_to 'Eliminar', driver, method: :delete, data: { confirm: '¿Está seguro de eliminar este conductor?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear Conductor', new_driver_path %>
